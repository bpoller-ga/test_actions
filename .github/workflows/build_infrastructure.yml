name: Create_Infra

on: [workflow_call]

jobs:
    build_infra:
        runs-on: ubuntu-latest
        steps:
            - name: Create Infra
              run: |
                  echo "I am creating the infra !"